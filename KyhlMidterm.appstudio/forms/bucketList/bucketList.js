/*
// Program 2
let bucketList = ''
list = []

do {
  bucketList = prompt('Would you like to see your bucket list, add an item, or stop the program? \n (type: `bucket list` or  `add item` or `stop`)')
  
  if (bucketList == 'bucket list') {
    console.log(`Here is your bucket list: \n${list}`)
  } else if (bucketList == 'add item') {
    let item = prompt('Enter a bucket list item to be added:')
    list.push(item)
  }
} while (bucketList !== 'stop')
  */